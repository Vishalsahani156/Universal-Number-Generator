FROM python:3.11-slim

WORKDIR /app

COPY backend/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY backend/app ./app
COPY shared ./shared

ENV PYTHONPATH=/app

CMD ["celery", "-A", "app.tasks.celery_app", "worker", "-Q", "generation,cleanup", "--concurrency=2"]
