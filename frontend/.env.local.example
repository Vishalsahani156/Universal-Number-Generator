# Relative URL (recommended): works on localhost and ngrok/mobile via Next.js proxy in next.config.mjs
NEXT_PUBLIC_API_URL=/api/v1
# Optional: override backend target for the Next.js proxy (default http://localhost:8000)
# BACKEND_URL=http://localhost:8000
